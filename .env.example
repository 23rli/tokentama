# Token Lens requires no environment configuration in normal VS Code use.
# Optional developer/test override for the VS Code workspaceStorage root:
TOKENLENS_COPILOT_WORKSPACE_STORAGE=

# The old ECO_COPILOT_WORKSPACE_STORAGE name is read temporarily for backwards
# compatibility but should not be used in new scripts.
